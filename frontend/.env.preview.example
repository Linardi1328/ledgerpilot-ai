# Placeholder-only Vercel preview frontend configuration.
# Do not commit real credentials, database URLs, tokens, client data, or production endpoints.
# Configure this value in Vercel Preview environment settings after a dedicated test backend exists.

BACKEND_INTERNAL_URL=https://ledgerpilot-test-backend.example.invalid
